@layout('main')

@section('content')
  <a href="/posts">GO Back</a>
  <hr>
  <h1>{{ post.title }}</h1>
  <p>{{ post.body }}</p>

  <hr>

  <a href="/post/edit/{{ post.id }}" class="btn btn-dark d-block float-left">Edit</a>
  <form action="{{ '/posts/' + post.id + '?_method=DELETE' }}" method="POST">
    {{ csrfField() }}
    <button type="submit" class="btn btn-danger d-block float-right">Delete</button>
  </form>
@endsection