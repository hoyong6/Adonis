@layout('main')

@section('content')
  <h1>{{ title }}</h1>
  <ul class="list-group">
    @each(test in tests)
      <li class="list-group-item">
        {{ test.eggvalue }}
      </li>
    @endeach
  </ul>
@endsection